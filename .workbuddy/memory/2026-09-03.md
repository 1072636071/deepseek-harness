# 2026-09-03

## DSH 系统提示词注入机制调研 + 学习资料 01 产出

- 应用户要求调研 DSH（本项目）系统提示词工作机制，产出 `docs/项目学习资料/01-DSH系统提示词注入机制.html`（单文件自包含 HTML，内联 CSS 零依赖）。
- 核心结论（均已在 HTML 中标注源码出处）：
  - `packages/core/system-prompt` = 注册表 + 装配流水线；`assemble()` 每 step 调一次，`renderPrompt()` 插值拼接 system 文本。
  - 四类原料：section（order 升序，第一方档位 `FIRST_PARTY_SECTION_ORDER` -1000~9900 稀疏预留）、context（动态事实，不进 system）、tools（schema）、variable（`{{name}}` 严格插值，出错即抛）。
  - 作用域分层：`agent.ctx` / 子代理 scope 遮蔽全局同名段（如 `deployment:persona`）；`complete: true` 段独占整条 system。
  - "运行还会注入"：`packages/core/agent-loop/src/runtime-context.ts` 的 `RuntimeContextProjection.project()` 每 step 把 sandbox/approval 等快照投为 user 消息 "Current runtime context…"，文本与上次相同则跳过；为空发清除标记。
  - `packages/context/agent-instructions`：AGENTS.md/CLAUDE.md 基线进 durable 上下文，read/write/edit 触碰文件后经 `agent/pre-step` 折入 inbox，`<system-reminder>` 包裹。
- `docs/项目学习资料/` 系列模板约定（写在 01 号 HTML 头部注释里）：命名 `NN-主题.html`；设计令牌 `--brand-500/--accent-500/--warn-500` 等；骨架 = meta 头块 → lede 结论 → h2 分节 → figure/callout/code/src-table/faq。
- 踩坑：agent-browser 在本机对 file:// URL 打开会卡死（守护进程无输出挂起），Windows 环境慎用；HTML 校验用 python html.parser 足够。

## 追加：实测"DSH 自带提示词啰不啰嗦"
- 结论：不啰嗦，设计上刻意反啰嗦。第一方段实测均为 1~2 句（tool:bash 仅 1 句，web_search 1 段约 370 字符）；persona 默认空；plan:policy 是部署方 config 且条件渲染；runtime 事实（沙箱/审批）根本不进 system。
- 关键设计注释：`tool-bash` 源码注明 "Cross-call guidance belongs in the prompt rather than one-call schema prose" —— 跨调用指引进 prompt，单次调用细节进工具 schema。
- 真正的"大头"来源：AGENTS.md（本仓库 18KB，远大于 harness 自带段）、第三方插件段、工具 schema 描述集。
- 精简开关：includeHarnessIdentity/includeRuntimeContext=false、suppressRuntimeContext()、persona complete:true 整条替换。
- 已把该实测分析补入 01 号 HTML 第 9 节（含目录项），HTML 校验通过（9 个 h2 分节）。
- 再补第 10/11 节（共 11 节）：第 10 节=各阶段真实注入原文（identity/harness:source/工具段 read-write-edit-glob-grep-bash-pwsh-web-lsp-session_query-workflow-ralph-report-terminal、sandbox 三档原文、approval 两档原文、agent-instructions 外壳、structured_output/team 段）；第 11 节=模式差异（工具呈现 native/ptc/both——ptc 只能调 run_code 且 system 注入 TS/Py 类型声明 renderToolsSdk、Plan 条件段、沙箱三档默认 read-only fail-safe、审批 ask/never、子代理/团队）。
- 关键源码定位：PTC 声明渲染 packages/core/tools/src/ts-types.ts renderToolsSdk / py-types.ts renderToolsSdkPy；模式判定 tools 模式 z.union(['native','ptc','both']) 默认 native。
- 【重构】应用户要求把单文件改为"多页小书"：新建 docs/项目学习资料/DSH提示词图解小书/（index 封面 + ch01~ch11 + assets/book.css 共享样式，含粘顶栏/章首/翻页器/键盘←→翻页/原文 quote-card 组件），旧单文件 01-*.html 已删除。MEMORY.md 系列约定已同步更新。

## 追加：个性化问候 grill（memorial 002→迁移）+ 姜晓插件 017 + 深度求索俏皮话池 003

- grill 会话「探索未至之境 → 时段+用户名问候」：前半在 `docs/memorial/002-hero-greeting-personalization`（已完结），后半用户指定落点 `E:\work\sp\dsh-web-ui-jx`（姜晓角色插件），新开其 `docs/memorial/017-hero-greeting-and-new-session-lines`（已完成）。
- 关键决策：宿主 `ui-conversation` 新增 slot `conversation.hero.headline`（3 处约 10 行，fallback 保原文案 → 宿主测试/i18n 零改动），dsh-web-ui-jx 加 peerDep 占用；时段 05/12/18/23 四档（wrap-around `h>=23||h<5`）浏览器本地时间，挂载算一次不挂 timer；用户名自填、trim 非空、上限 16 字、存 client localStorage（绕开 host settings memory 模式不可写）；姜晓台词称「大人」不带用户名；新建会话台词判定 `sessions.list` 的 `byId[current].blank === true`（`SessionSummary.blank` 是现成字段）；颜文字全量覆盖、线条型冷感。
- 017 产出：4 条 ADR（0033–0036，已同步该仓库 `docs/adr/`）+ 台词草案（8 状态 + 4 惊吓池全加颜文字 + 新建会话 4 句）；CONTEXT.md 已回写术语与已定决策。
- 本仓库新开 `docs/memorial/003-deep-diving-playful-pool`（已完成待实施）：`chat.deepDiving`「深度求索中...」换随机俏皮话池。决策：池子内置 `ui-chat/src/client/locale.ts`（locale-owner 豁免 i18n 门禁）；每轮一条（useState 初始化器 + random 注入）；zh/en 各 100 条按 `LocaleFace.snapshot.active` 分桶；保留原 key 作兜底；3 处测试断言需重写。文案池在 `docs/memorial/003-deep-diving-playful-pool/pool.md`，待用户过目后实施（PR 附 Agent Note）。
- 事实：`verify-client-ui-i18n` 只豁免 locale.ts/locales.ts//locales/ 路径内的文案；`ui-chat` 的 TurnStatus 每秒重渲染，15 秒后追加 `formatRunDuration` 时长。

## 追加：PRD 发布（jxx-to-spec）

- deepseek-harness：`.scratch/03-hero-headline-slot/PRD.md`（宿主开槽，前置依赖）+ `.scratch/04-deep-diving-playful-pool/PRD.md`（俏皮话池），均 `Status: ready-for-agent`。
- dsh-web-ui-jx：`.scratch/25-hero-greeting-and-new-session-lines/PRD.md`（占用 slot + 新建会话台词 + 全量颜文字 + 设置卡名字/开关），`ready-for-agent`，前置依赖宿主 03。
- seam 决策：全部复用既有 seam（slot 契约 / overlay-speech 纯逻辑决策器 / createPersistentSetting / sessions.list 快照 / locale-owner 文件），未新建 seam 类型。

## 追加：工单拆解发布（jxx-to-tickets）

- deepseek-harness：`.scratch/03-hero-headline-slot/issues/01-add-hero-headline-slot.md`、`.scratch/04-deep-diving-playful-pool/issues/01-deep-diving-random-pool.md`（各 1 张，无阻塞）。
- dsh-web-ui-jx：`.scratch/25-hero-greeting-and-new-session-lines/issues/` 5 张——01 占 slot+时段问候 MVP（无阻塞，外部前置宿主 03）→ 02 用户名 → 03 开关；04 新建会话台词（blocked by 01，复用时段判定）；05 全量颜文字（无阻塞）。附 `MILESTONES.md`（M0 宿主前置 / M1 问候 / M2 个性化 / M3 台词）。
- 全部 `Status: ready-for-agent`，按 `/jxx-implement` 逐前沿推进。

## 追加：工单实现与审查收口（jxx-implement + jxx-code-review 并行团队）

- deepseek-harness 已提交：`1e1060e7`（hero headline slot）、`48f13a86`（俏皮话池）、`57165974`（owner 向占用者传 headline className）。两工单各附中英双语 Agent Note + i18n.yaml sidecar（verify-translation-pairing --write 记录），Status: done。
- dsh-web-ui-jx 已提交：`e7234e3`（全量颜文字 + 气泡 max-width 放开，工单 05 done）、`a24726d`（memorial 017 + ADR-0033~0036 + tracker 文档）。npm test 632 用例、verify 24 项全绿。
- 审查结论（4 维并行 subagent）：harness 标准=无硬违规（i18n 豁免实跑验证成立；random 注入缝暂未被测试使用——测试用 vi.spyOn(Math.random)，记录于 Agent Note）；harness spec=无发现项；jx 标准=无发现项；jx spec=核心逐字符合，仅文档归属与气泡宽度渲染测试缺失两个非阻塞项。
- 修复记录：实现 agent 初版有 3 处 typecheck 错误（locale.ts 池查找的类型收窄 `localeId in deepDivingPool` + 测试 `zh[0]!` 非空断言），主线程修复后全绿。
- 环境坑（重要）：子 agent 跑 pnpm install 会把 node_modules 链接装成 node 无法解析的形态（UNKNOWN 错误）；主线程 `pnpm install` 可重建为正常 Junction。esbuild@0.21.5 postinstall 下载失败（企业 TLS 代理），不影响测试与门禁。
- 集成事实：npm 上 `@deepseek-ai/dsh-client-ui-conversation@0.1.2-alpha.5`（9/2 发布）不含新 slot → jx 插件暂不加 peerDep，用运行时常量占用（ADR-0033 临时形态），宿主发新版后切换。jx 25/01（时段问候 MVP）实现 agent 进行中。

## 追加：25 号全部工单完成（里程碑收口）

- dsh-web-ui-jx 五张工单全部 done 并提交：`7f36d2e`（01 时段问候 MVP）、`cc0ac9b`（02 用户名+带名问候）、`8eec221`（04 请安台词）、`d93574e`/`cedcb84`（tracker）、`7a1a549`（03 总开关）、`e7234e3`（05 颜文字）、`494034a`（文档同步+里程碑勾选）。
- 语义要点：开关键 `jx-greeting-enabled`（默认开）；关闭=注销 slot 占用回落宿主文案 + new-session greeter evaluate 实时静默（始终订阅，中途切换立即生效，不追溯历史——D13）；名字键 `jx-user-name`（trim/≤16字/剥离控制字符）；台词一律「大人」不带用户名（ADR-0034）。
- 合并态独立验证：typecheck / 682 用例 / build / verify 24 项全绿（client.js 147KB ≤512KB）。
- 团队协作观察：部分审查/提交由团队成员（rev-jx02/rev-jx03/rev-jx04/impl-jx-new-session 等）自行完成并直接 commit——最终由主线程核对 git log 与工单状态收口；rev-jx03 的报告始终未回传，03 的标准+spec 审查结论由 rev-jx04 交叉覆盖 + 主线程实读 diff 兜底。
- 经验：通知常不带正文，需主动 SendMessage 索要；多 agent 并发写同一仓库时 build 产物与提交时序会互相踩，主线程用「读 git log + 工单状态」做最终事实核验最可靠。

## 追加：25 号 5 张工单全部实现+审查+提交完毕

- dsh-web-ui-jx 提交链：`e7234e3`(05 颜文字) → `a24726d`(docs) → `7f36d2e`(01 slot 占用+时段问候) → `cc0ac9b`(02 用户名+带名) → `cedcb84`(tracker 01/02 done) → `8eec221`(04 新建会话请安) → `d93574e`(tracker 04 done) → `7a1a549`(03 问候开关)。deepseek-harness：`1e1060e7`/`48f13a86`/`57165974`。全部工单 Status: done。
- jx 插件运行形态：slot 占用经本地 SlotMap 模块扩充（ADR-0033 临时形态，SWITCH 条件注释在 hero-headline-greeting.ts）；hero 落点单一收敛在 registerHeroHeadlineGreeting 的 inject/dispose（关→dispose，宿主回落 fallback）；新建会话经 `isNewSessionGreetingEnabled` 单点接开关。
- 审查发现与修复：01 审查建议 JSX 惯例+类型归位（并入 02 修正）；03 首轮 2 项阻断（SettingsCard 重复粘贴、HeroHeadline hooks 违规）由主线程按审查建议修复，复检结论三次未回传，按首轮报告自定放行条件收口（工单评论有全程记录）。次要遗留：bool parse 手写副本 5 份、hero 占用渲染测试缺位——后续收敛项。
- 协作教训：subagent 的审查/实现报告多次只回完成通知不回正文，需 SendMessage 催取；催取后有时仍只回通知——重要结论要主动复查实盘（git diff）而非依赖回传。
- 最终验证基线：jx test 682 passed / build 双半区 / verify 24 项；harness skeleton 22 + chat-view 76 + typecheck + i18n 门禁。

## 追加：收尾提交

- harness 又提交 `598782cf`(memorial 002/003 + pool.md) 与 `785ecf6c`(补入上次会话遗留：模型可见性 tracker + 项目学习资料；修正两处 EOF 空行)。jx 工作树干净。
- 仅剩 `.workbuddy/` 未提交（WorkBuddy 自身状态/会话记忆，非项目源码，未提交）。
- 门禁经验：lefthook 的 whitespace(staged) 会拦「EOF 多余空行」，且失败时只显示 hook 摘要——提交后务必 `git log -1` 复核是否真成提交；本项目已多次出现「hooks 通过但提交未生成」。
- peerDep 切换（ADR-0033）：宿主需发含 slot **且含 className owner prop**（commit 57165974）的新版，jx 才能加 devDep、删本地 `declare module` 扩充；切换前功能在本地 host 源码下已可用（运行时字符串占用同键）。
